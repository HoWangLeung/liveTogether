import { StyleSheet } from "react-native";

const commonStyles = StyleSheet.create({
  root: {
    alignItems: "center",
    padding: 20,
  },

  inputSpace: {
    marginTop: 10,
    marginBottom: 10,
    width: "100%",
  },
  sectionTitle: {
    marginRight: "auto",
    fontWeight: 600,
    fontSize: 20,
    marginTop: 10,
  },
  titleLarge:{
    fontWeight:600,
    paddingLeft:15,
    paddingBottom:5,
    paddingTop:5
  }
});

export default commonStyles;
